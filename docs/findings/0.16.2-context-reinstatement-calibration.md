# 0.16.2 context reinstatement calibration

**Fixture:** Redis/payments diagnostic observations (`tests/test_016_2_context_reinstatement.py`)

**Query:** “Why did we decide not to use Redis?”

**Retrieval context:** payments-api architecture-decision cue (full `_RETRIEVAL_CONTEXT`)

## Weight sweep

| `context_reinstatement_weight` | Payments match strength | Applied contribution | Payments activation rank vs weight=0 |
|--------------------------------|-------------------------|----------------------|--------------------------------------|
| 0.00 | 1.0 | 0.0 (disabled) | baseline |
| 0.25 | 1.0 | 0.25 | improves |
| 0.50 | 1.0 | 0.50 | improves (default) |
| 0.75 | 1.0 | 0.75 | improves |
| 1.00 | 1.0 | 1.00 | improves |

Observations from in-repo parametrized tests:

- Payments-api episode: `ContextMatch.score ≈ 1.0`, auth-api `≈ 0.17`, analytics-api domain dimension `0.0`.
- All weights `{0.25, 0.50, 0.75, 1.00}` apply reinstatement to payments without admitting unrelated cafeteria content in adversarial fixture.
- `0.25` is the smallest tested weight that measurably improves payments rank vs `0.00` on the fixture.
- **`0.50` is the selected operational default** for headroom on partial-match cues and incomplete cue coverage without unrelated admission in tested fixtures.

## Engineering note

`context_reinstatement_weight` is an in-repo engineering calibration, not a cognitive-science constant. External CogKuraBench workloads without retrieval context should remain equivalent to `0.16.1` aside from additive diagnostic fields.
